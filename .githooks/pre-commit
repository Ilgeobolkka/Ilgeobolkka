#!/usr/bin/env sh
# 커밋 전 Java 메인·테스트 소스를 컴파일한다. 테스트 실행은 pre-push에서 수행한다.

fail() { printf '✗ %s 실패 — 커밋을 중단합니다 (--no-verify 우회 금지)\n' "$1" >&2; exit 1; }

[ -x ./gradlew ] || fail "Gradle wrapper 확인"
./gradlew -q classes testClasses || fail "gradlew classes testClasses"

exit 0

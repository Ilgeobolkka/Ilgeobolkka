spring:
  main:
    web-application-type: none

content-import:
  manifest: ${CONTENT_IMPORT_MANIFEST:fixtures/content/manifest.json}
  output-root: ${CONTENT_IMPORT_OUTPUT_ROOT:var/content/pages}
  pdftotext-command: ${PDFTOTEXT_COMMAND:pdftotext}
  pdftoppm-command: ${PDFTOPPM_COMMAND:pdftoppm}
